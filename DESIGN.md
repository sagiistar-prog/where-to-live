# DESIGN.md

## Design Register

product

## Physical Scene

用户刚看完房、收到 offer、准备付款或正在比较城市。TA 需要快速判断，不想被复杂配置和冗长解释打断。界面应该像一个浅色、清醒、可信的决策工作台：第一眼知道输入什么，第二眼知道结果怎么看，第三眼知道下一步怎么做。

## Visual Direction

浅色商业化 AI 工具界面，启动页可参考 Notion AI / Vidu 一类“中心输入 + 直接分流 + 结果预览”的结构，但视觉表达要服务居住决策，不做炫技型 AI 视觉。

整体气质：专业、干净、克制、可展示给产品经理、客户和投资人。页面看起来像真实可上线产品，不像作品集模板。

## Color Strategy

Restrained light product UI.

- 背景：浅灰白或轻微冷灰，不使用纯白铺满所有层级。
- 内容层：白色和浅灰卡片，用细边框、阴影和留白区分层级。
- 主色：克制青绿色或蓝绿色，用于主操作、当前状态和可信行动。
- 风险色：琥珀、红色、绿色只用于谨慎、暂停、通过等状态。
- 不使用大面积紫蓝渐变。
- 不使用渐变文字。
- 不使用暗色大背景，除非是局部登录视觉或报告强调区域。

## Typography

- 字体：系统 sans 字体栈，优先稳定和可读性。
- 标题直接说明用户正在解决的问题，不用营销化大词。
- 启动页主标题可以更强，但工具页标题要紧凑。
- 表格、报告和表单允许较高信息密度。
- 正文行长控制在可读范围，避免大段解释。
- 文案专业、直白，不口语化，不使用夸张修辞。

## Layout

- 启动页：中心输入框优先，下面露出关键工具入口和结果预览。
- 产品内页：顶部导航 + 左侧或内容区任务入口，避免全页同尺寸卡片堆叠。
- 工作台：突出当前行动队列、最近判断、关键风险和可继续动作。
- 表单：按真实决策顺序排列字段，复杂判断优先做成表单模式。
- 卡片只用于独立任务、报告模块、表格容器、价格套餐和设置面板。
- 不嵌套卡片，不把页面 section 做成装饰性浮动卡片。
- 数据表格可以横向滚动，但页面本体不应出现无意义横向溢出。

## Components

- Button：统一高度和圆角，主按钮必须说明下一步动作。
- Card：8px 圆角以内，边框和背景层级一致。
- Badge：只表达状态，不做装饰。
- Form：标签清晰，字段和 placeholder 要像真实用户会填写的内容。
- Table：用于候选对比、成本拆解和确认事项，不用装饰性卡片替代表格。
- Report section：结论、依据、风险、下一步、可复制文本保持稳定结构。
- Pricing card：套餐数量少，价格、额度、适用人群和取消规则清楚。

## Motion

- Motion serves state.
- 页面进入可以轻微淡入，但不编排复杂加载动画。
- Hover 和状态变化 150 到 250ms。
- 不使用 bounce 或 elastic。
- 动画不能拖慢决策。

## Interaction

- 启动输入支持一句话开始，并把城市、预算、租金、通勤、付款和合同担忧带入下游页面。
- CTA 文案必须明确，例如“生成体检报告”“开始付款咨询”“补第二套候选”“复制沟通文本”。
- 风险结论必须同时给出原因和行动。
- 空状态必须告诉用户下一步怎么做，不展示内部解释。
- 结果页要给出可复制文本或明确跳转，不让用户自己重新组织语言。

## Accessibility

- 文本对比满足浅色背景阅读。
- 触控目标至少 44px。
- 所有输入有 label。
- 图标按钮有 aria-label。
- 状态不能只依赖颜色，需要有文字标签。

## Bans

- 不使用纯黑和纯白作为大面积背景。
- 不使用渐变文字。
- 不使用装饰性光球、泡泡、大片抽象渐变。
- 不做重复的同尺寸 icon card 海。
- 不把报告做成营销海报。
- 不用过度拟人、保证式文案或平台中介口吻。
- 不在用户界面展示内部服务商、密钥、调用策略或成本控制说明。
