# DESIGN.md

## Design Register

product

## Physical Scene

一个 24 岁到 32 岁的年轻人在晚上 10 点，刚看完房或刚收到 offer，坐在笔记本前用住哪儿 AI 做决策。环境偏暗，用户焦虑但需要清醒判断，所以界面选择深色、低眩光、信息密度适中、强调可信与可执行。

## Visual Direction

深色沉浸式决策台，克制的青绿色作为主操作和状态线索，玫红只用于局部风险提示或强调。整体要高级、冷静、可信，不做炫技型 AI 视觉。

## Color Strategy

Restrained product UI.

- 背景：带冷青色倾向的深色，不使用纯黑。
- 内容层：深色面板和轻微透明层级，用边框和空间区分层次。
- 主色：青绿色，用于主要按钮、当前状态、进度和可信行动。
- 风险色：琥珀、红色、绿色用于谨慎、拒绝、建议状态。
- 不使用渐变文字。
- 不使用高饱和大面积紫蓝渐变。

## Typography

- 字体：系统 sans 字体栈，优先稳定和可读性。
- 产品 UI 不使用 display 字体。
- 标题用于建立层级，不做超大营销化排版。
- 表格和报告允许较高信息密度。
- 正文行长控制在可读范围，报告段落不超过约 75 个中文字符的视觉长度。

## Layout

- 顶部导航 + 侧边导航用于产品内页面。
- 工作台可使用信息模块，但避免所有内容都是同尺寸 icon card。
- 卡片只用于独立任务、报告模块、表格容器和设置面板。
- 不嵌套卡片。
- 用 tight grouping 表达同一任务，用更大 section gap 表达阶段变化。
- 数据表格可横向滚动，但移动端页面本体不应出现无意义横向滚动。

## Components

- Button：统一圆角、统一高度，主按钮用于下一步动作。
- Card：8px 圆角以内，边框和背景层级一致。
- Badge：只表达状态，不做装饰。
- Form：标签清晰，字段按真实决策顺序排列。
- Table：用于可比较信息，不用装饰性卡片替代表格。
- Report section：标题、图标、内容列表保持一致。

## Motion

- Motion serves state.
- 页面进入可以有轻微淡入，但不要编排复杂加载动画。
- Hover 和状态变化 150 到 250ms。
- 不使用 bounce 或 elastic。
- 尊重用户工作流，动画不能拖慢决策。

## Interaction

- CTA 文案必须明确下一步，例如“生成体检报告”“继续筛选片区”“识别风险条款”。
- 风险结论必须同时给出原因和行动。
- 表单字段默认使用真实示例值，降低理解成本。
- 空状态要告诉用户下一步怎么做。

## Accessibility

- 文字对比要满足深色背景阅读。
- 触控目标至少 44px。
- 所有输入有 label。
- 图标按钮有 aria-label。
- 状态不能只依赖颜色，需要有文字标签。

## Bans

- 不使用纯黑和纯白。
- 不使用渐变文字。
- 不把玻璃拟态当默认装饰。
- 不做重复的同尺寸 icon card 海。
- 不把报告做成营销海报。
- 不用过度拟人或保证式文案。
